/*
  TODO: Do some logic if it is necesary.
*/
const logout = () => {
  try {
    return null;
    // eslint-disable-next-line no-unreachable
  } catch (err) {
    throw err;
  }
};

export default logout;
